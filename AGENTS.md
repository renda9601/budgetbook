# AGENTS.md

이 문서는 이 저장소에서 작업하는 Codex와 다른 개발 에이전트가 반드시 따라야 할 프로젝트 규칙이다. 저장소 전체에 적용한다. 사용자 요청과 이 문서가 충돌하면 사용자에게 충돌 내용을 먼저 알리고 확인을 받는다.

## 1. 프로젝트 정체성과 우선순위

- 이 프로젝트의 중심 제품은 **부부가 함께 사용하는 가족용 공동 가계부 웹앱**이다.
- 기본 사용자는 `dad`와 `mom`이며, 거래를 사용한 사람과 거래를 입력한 사람을 구분할 수 있어야 한다.
- 두 사용자는 같은 가족(`household`) 데이터에 접근한다. 권한과 데이터 소유 관계를 흐리게 만들지 않는다.
- 주 기술 구조는 **Next.js + React + JavaScript + Supabase**다.
- 초기 배포 대상은 Vercel이다. 단, 사용자가 요청하기 전에는 배포하거나 배포 설정을 변경하지 않는다.
- PC, Tablet, Mobile은 같은 DB, 같은 거래 원본, 같은 업무 규칙을 사용한다. 기기마다 별도의 데이터 구조나 별도 기능 복제품을 만들지 않는다.
- 화면 크기에 따라 자연스럽게 배치가 바뀌는 반응형 UI로 구현한다.
- 편한가계부(Realbyte)는 정보 구조와 사용 흐름을 이해하기 위한 UX 참고 자료일 뿐이다. 화면, 문구, 색상, 아이콘, 배치를 그대로 복제하지 않는다.

## 2. 현재 저장소 이해

- `app/`, `lib/`, `supabase/`는 가족 가계부 웹앱의 중심 영역이다.
- `src/`, `config/`, `tests/`, `data/`, `logs/`에는 우리은행 거래내역 수집 도구가 섞여 있다.
- 은행 사이트 직접 크롤링은 가족 가계부의 핵심 기능이 아니다. 요청 없이 이 도구를 확장하거나 가계부 흐름에 연결하지 않는다.
- 기존 `README.md`는 현재 은행 수집 도구 중심이며, `MVP_WORK_SUMMARY.md`와 `docs/ACCOUNT_BOOK_DIAGNOSIS.md`는 가족 가계부 상태를 설명한다. 작업 전 관련 문서를 함께 읽고 한 문서만 보고 프로젝트 전체를 판단하지 않는다.
- 현재 코드에서 확인되지 않는 기능이나 운영 상태는 추측하지 않는다. 필요하면 **확인되지 않음**이라고 보고한다.

## 3. 기술 규칙

- Next.js App Router와 React 구조를 유지한다.
- 현재 프로젝트는 JavaScript 프로젝트다. 사용자의 명시적 요청 없이 TypeScript로 바꾸거나 `.js` 파일을 `.ts`/`.tsx`로 전환하지 않는다.
- Supabase를 인증과 가족 가계부 DB의 기본 저장소로 사용한다.
- Supabase 클라이언트, Auth, RLS(Row Level Security, 로그인 사용자별 데이터 접근 제한)를 변경할 때는 보안 영향을 함께 검토한다.
- 공개 가능한 Supabase anon key와 비밀 관리 키를 구분한다. service role key나 다른 비밀 키를 브라우저 코드에 넣지 않는다.
- 새로운 라이브러리는 기존 코드와 표준 웹 기능으로 해결하기 어려우며 꼭 필요한 경우에만 추가한다.
- 라이브러리 추가가 필요하면 먼저 필요성, 대안, 번들 크기와 유지보수 영향을 설명한다. 사용자 승인 범위를 벗어나 임의 설치하지 않는다.
- 한 번의 작업에서는 사용자가 요청한 범위만 수정한다. 관련이 있어 보인다는 이유만으로 주변 기능까지 함께 고치지 않는다.

## 4. 가족과 사용자 규칙

- `dad`와 `mom` 구분을 보존한다.
- 화면 표시 이름과 실제 인증 사용자 ID를 같은 값으로 가정하지 않는다.
- 가능하면 거래를 사용한 사람과 거래를 입력한 로그인 사용자를 별도로 다룬다.
- 사용자, 가족, 거래의 관계를 변경할 때는 기존 `households`, `profiles`, `household_members` 구조와 실제 Supabase 적용 상태를 먼저 확인한다.
- 부부 공동 데이터라도 권한 검사를 생략하지 않는다. 구성원 관리 권한과 거래 관리 권한을 구분한다.
- 사용자 ID, 실명, 이메일을 새 코드 여러 곳에 반복 하드코딩하지 않는다. 기존 하드코딩을 발견해도 요청 없이 대규모 정리하지 말고 위험을 보고한다.

## 5. 거래와 데이터 규칙

- 모든 통계와 보고서는 가능한 한 실제 거래 원장을 기준으로 계산한다.
- 수입, 지출, 이체를 서로 다른 거래 유형으로 유지한다.
- 이체는 수입이나 지출로 중복 합산하지 않는다. 이체 구조를 바꿀 때는 출발 자산과 도착 자산을 모두 표현하는 계획을 먼저 제시한다.
- 현금, 카드, 은행 계좌, 지역화폐 개념을 유지한다.
- 카테고리는 수입/지출 구분과 향후 대분류/소분류 확장을 고려한다.
- CSV/Excel 가져오기는 SMS 자동수집보다 먼저 고려한다.
- 외부 자료를 가져올 때는 데이터 출처와 중복 거래를 구분할 방법을 설계한다.
- 금액, 날짜, 사용자, 자산, 카테고리 계산 규칙을 바꾸면 기존 월별 합계와 전월 비교에 미치는 영향을 검증한다.
- mock 데이터(화면 확인용 가짜 데이터)와 실제 Supabase 데이터를 명확히 구분한다. 데모 데이터를 실제 운영 데이터처럼 취급하지 않는다.

## 6. DB와 migration 규칙

- 기존 DB 테이블, 열, 정책을 임의로 삭제하거나 rename(이름 변경)하지 않는다.
- DB 변경이 필요하면 코드를 먼저 바꾸지 말고 다음 내용을 포함한 migration 계획을 사용자에게 보고한다.
  1. 변경 목적
  2. 현재 구조와 변경 구조
  3. 영향을 받는 테이블·열·RLS 정책
  4. 기존 데이터 보존과 변환 방법
  5. 되돌리는 방법
  6. 앱 코드 변경 범위
  7. 검증 방법
- 사용자의 승인 전에는 migration 파일 생성, SQL 실행, seed 실행, Supabase 테이블·정책 변경을 하지 않는다.
- DB 구조를 판단할 때 저장소의 SQL만 보고 실제 원격 DB에 적용되었다고 가정하지 않는다.
- 파괴적인 변경보다 새 필드 추가, 데이터 이전, 검증, 구 필드 제거의 단계적 방식을 우선한다.
- RLS를 끄거나 넓은 권한으로 문제를 우회하지 않는다.

## 7. UI/UX와 반응형 규칙

- 기존 기능과 UI를 사용자의 명시적 요청 없이 전면 재설계하지 않는다.
- 작은 기능 요청을 이유로 전체 색상, 레이아웃, 메뉴 구조, 컴포넌트 체계를 바꾸지 않는다.
- PC는 좌측 메뉴와 메인 콘텐츠를 기본으로 고려하고, 필요할 때만 입력/상세 패널을 사용한다.
- Mobile은 하단 메뉴와 모바일에 맞는 거래 입력 화면 또는 Bottom Sheet(화면 아래에서 올라오는 패널)를 고려한다.
- Tablet은 PC 화면을 단순 축소하지 말고 중간 폭에서도 입력과 목록을 사용할 수 있어야 한다.
- PC, Tablet, Mobile에서 기능과 데이터 의미는 같아야 한다.
- UI 변경 시 최소한 좁은 화면과 넓은 화면에서 글자 겹침, 가로 넘침, 하단 메뉴 가림, 터치 영역을 확인한다.
- 편한가계부의 핵심 흐름인 “전체 현황 → 원인 → 카테고리 → 실제 거래”는 참고할 수 있지만 독자적인 화면과 문구를 사용한다.

## 8. 보안과 개인정보 규칙

- 비밀번호, 계좌번호, 생년월일, 인증서 비밀번호, API 비밀 키를 소스·문서·로그에 기록하지 않는다.
- 비밀번호를 localStorage, sessionStorage 또는 일반 쿠키에 평문으로 저장하지 않는다.
- 환경변수 값을 콘솔이나 보고서에 출력하지 않는다. 점검할 때는 변수 이름과 설정 여부만 확인한다.
- 은행 사이트의 CAPTCHA, 보안 키패드, 인증서, 자동화 탐지, 개발자도구 탐지 또는 기타 보안 장치를 우회하지 않는다.
- 은행 사이트 자동 로그인, 자격증명 자동 입력, 내부 API 역분석, 네트워크 가로채기를 구현하지 않는다.
- 은행이 자동화를 차단하거나 경고하면 우회하지 않고 중단한다.
- SMS 자동수집은 사용자가 별도 단계로 명시적으로 요청한 경우에만 검토한다. 그 전에는 구현하거나 권한을 추가하지 않는다.
- 가족 금융 데이터가 들어간 CSV, Excel, 로그, 이미지의 저장 위치와 Git 추적 여부를 반드시 확인한다.

## 9. 기존 코드 보호와 작업 범위

- 기존 기능은 명시적 요청 없이 삭제하지 않는다.
- 사용하지 않는 것으로 보이는 코드도 바로 삭제하지 않는다. 호출 여부, 문서, 향후 계획을 확인하고 먼저 삭제 후보로 보고한다.
- 기존 파일을 이동하거나 이름을 바꿀 때는 import, 실행 스크립트, 문서, 배포에 미치는 영향을 확인한다.
- 사용자 작업이 남아 있는 dirty worktree(커밋되지 않은 변경이 있는 상태)를 존중한다. 관련 없는 기존 변경을 되돌리거나 덮어쓰지 않는다.
- 요청한 파일과 직접 관련된 최소 범위만 수정한다.
- 리팩터링(동작은 유지하고 코드 구조만 정리하는 작업)은 기능 변경과 섞지 않는다. 필요하면 별도 작업으로 제안한다.
- 오류를 발견해도 현재 요청이 진단이나 문서 작업이면 기능 코드를 고치지 않고 위치, 영향, 권장 방향만 보고한다.

## 10. 검증과 완료 보고

- 변경 전에 관련 코드, 기존 문서, DB 구조를 먼저 읽는다.
- 기능을 수정한 경우 변경 범위에 맞는 lint, 테스트, 빌드 또는 수동 검증을 수행한다.
- 검증 명령이 파일 생성, DB 변경, 외부 접속 등 추가 영향을 만들면 먼저 그 영향을 확인한다.
- 테스트가 없거나 실행하지 못했으면 성공했다고 추측하지 말고 그 사실과 이유를 밝힌다.
- 작업 완료 후 다음 내용을 한국어로 정리한다.
  - 변경한 파일
  - 각 파일을 변경한 이유
  - 사용자가 확인할 핵심 동작
  - 수행한 검증과 결과
  - 남은 위험 또는 확인되지 않은 사항
- Git commit은 사용자가 요청한 경우에만 수행한다.
- Git commit 메시지는 변경 내용을 이해할 수 있도록 구체적인 한국어로 작성한다.

## DO NOT

다음 행동은 사용자의 명시적 요청과 필요한 사전 확인 없이 절대로 하지 않는다.

- 기존 기능, 파일, 폴더, DB 데이터를 삭제하지 않는다.
- 기존 UI를 전면 재설계하거나 편한가계부 화면을 그대로 복제하지 않는다.
- JavaScript 프로젝트를 TypeScript로 임의 전환하지 않는다.
- Next.js + React 구조를 다른 프레임워크로 임의 교체하지 않는다.
- PC, Tablet, Mobile용 DB나 업무 로직을 따로 만들지 않는다.
- 기존 DB 테이블·열·RLS 정책을 삭제하거나 rename하지 않는다.
- 승인 없이 migration을 만들거나 실행하지 않는다.
- 승인 없이 Supabase schema, seed, Auth 사용자, RLS 또는 운영 데이터를 변경하지 않는다.
- 문제 해결을 위해 RLS를 끄거나 service role key를 브라우저에 노출하지 않는다.
- 꼭 필요하다는 근거 없이 라이브러리를 추가·삭제하거나 대규모 버전 업그레이드를 하지 않는다.
- 요청 범위를 넘어선 리팩터링, 파일 이동, 폴더 재구성, 포맷 전체 변경을 하지 않는다.
- 기존 mock 데이터를 운영 데이터로 옮기거나 운영 데이터라고 가정하지 않는다.
- 비밀번호나 금융 개인정보를 브라우저 저장소, 코드, Git, 문서, 로그에 저장하지 않는다.
- 은행 사이트 보안 우회, 자동 로그인 크롤링, 자격증명 자동 입력, CAPTCHA 우회, 내부 API 역분석을 구현하지 않는다.
- 별도 요청 없이 SMS 자동수집을 구현하지 않는다.
- 사용자의 요청 없이 Vercel 배포, Git commit, Git push를 하지 않는다.
- 검증하지 않은 기능을 완료되었다고 보고하지 않는다.
- 코드에서 확인되지 않은 사실을 추측하여 문서나 구현의 전제로 사용하지 않는다.
